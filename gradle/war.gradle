apply plugin: "war"

bootWar {
    mainClass = "com.ipa.catelogbook.IpaCatalogBookServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
